apiVersion: v1
kind: Service
metadata:
  name: txt2python
  labels: { app: txt2python }
spec:
  type: ClusterIP
  selector: { app: txt2python }
  ports:
    - name: http
      port: 80
      targetPort: 8000
